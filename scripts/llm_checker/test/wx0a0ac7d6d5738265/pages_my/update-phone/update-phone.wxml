<view class="wrap">
    <zym-navigation bind:__l="__l" uI="b07523ee-0" uP="{{a}}" wx:if="{{a}}"></zym-navigation>
    <view class="cont">
        <view class="upPhone">
            <view class="phonet"> 修改手机号码 </view>
            <view class="slectipt">
                <view class="iocn"></view>
                <input bindinput="{{c}}" class="ipt" maxlength="11" placeholder="请输入新手机号" placeholderClass="palce" type="number" value="{{b}}"></input>
            </view>
            <view class="phones btns" wx:if="{{d}}">
                <input bindinput="{{f}}" class="input" maxlength="6" placeholder="请输入验证码" type="number" value="{{e}}"></input>
                <text bindtap="{{h}}" class="btnco" wx:if="{{g}}">获取验证码</text>
                <text class="btnco" wx:else>{{i}}秒后重发</text>
            </view>
            <view class="error" wx:if="{{j}}"> 手机号格式错误 </view>
            <button bindgetphonenumber="{{k}}" class="phonebtn" lang="zh_CN" openType="getPhoneNumber">获取手机号</button>
        </view>
        <button bindtap="{{m}}" class="update" lang="zh_CN" wx:if="{{l}}">确认修改</button>
        <button bindtap="{{n}}" class="update" lang="zh_CN" wx:else>确认修改</button>
    </view>
</view>
