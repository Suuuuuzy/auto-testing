<view class="container">
  <view class="main">
    <view class="user-info">
      <image class="user-image" src="/assets/img/logo.png"></image>
      <view class="account">火瞳刷脸消费</view>
    </view>
    <view class="login-form">
      <van-cell-group>
        <van-field
          clearable
          bind:blur="phoneBlur"
          border="{{false}}"
          value="{{phone}}"
          placeholder="请输入手机号"
        ></van-field>
        <van-field
          center
          clearable
          useButtonSlot
          bind:blur="imageCodeInput"
          border="{{false}}"
          value="{{imageCode}}"
          placeholder="请输入图形证码"
        >
          <image
            bindtap="getImageCode"
            class="imageCode"
            mode="aspectFit"
            slot="button"
            src="{{imageCodeUrl}}"
          ></image>
        </van-field>
        <van-field
          center
          clearable
          useButtonSlot
          bind:blur="smsCodeInput"
          border="{{false}}"
          value="{{smsCode}}"
          placeholder="请输入短信验证码"
        >
          <van-button
            plain
            bind:click="sendSms"
            size="small"
            slot="button"
            type="info"
            wx:if="{{!isSent}}"
            >发送验证码</van-button
          >
          <van-button
            disabled
            plain
            size="small"
            slot="button"
            type="info"
            wx:else
            >{{countdownText}}</van-button
          >
        </van-field>
      </van-cell-group>
    </view>
    <van-button
      block
      bind:click="login"
      color="linear-gradient(to right, #3680FD, #7659FC)"
      id="login"
      size="normal"
      type="primary"
      >登录</van-button
    >
    <view class="tip"
      >此小程序为使用火瞳人脸识别机进行企业内部福利管理的企业，提供菜谱查看、消费记录查看等服务，请确认后登录。</view
    >
  </view>
  <van-toast id="van-toast"></van-toast>
</view>
